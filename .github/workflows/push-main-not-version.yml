name: push-main-github-actions
run-name: push-main-github-actions
on:
  push:
    branches:
      - 'main'
    tags-ignore::
      - 'v[0-9]+.[0-9]+'
jobs:
  processing:
    runs-on: self-hosted
    steps:
      - id: checkout
        uses: actions/checkout@v3
      - id: build
        uses: ./.github/actions/build
      - id: test
        uses: ./.github/actions/test
        with:
          version: ${{ steps.build.outputs.version }}
      - id: pack
        uses: ./.github/actions/pack
        with:
          version: ${{ steps.build.outputs.version }}